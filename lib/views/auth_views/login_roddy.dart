import 'package:flutter/material.dart';

class LogInToRoddy extends StatelessWidget {
  const LogInToRoddy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
